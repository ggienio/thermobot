#include "movement.h"
#include "config.h"

void Movement::move(float vx, float vy, float omega) {
    // TODO: implement
    // convert vx, vy and omega [-1, 1] to the range accepted by pwm [0, Motors::SPEED_CAP]
    // call appropriate methods of each motor so that they move in the desired way
}